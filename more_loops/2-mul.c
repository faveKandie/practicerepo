#include <stdlib.h>
#include <stdio.h>
/**
 * main - function that multiplies two integers. Returns the value of the multiplication
 * Return: result Success
 */
int main(void)
{
	int num1, num2, result;

	for (num1 = 0; num1 >= 10; num++)
	{
		for (num2 = 0; num2 >= 10; num2++)
		{
			result = num1 * num2;

			_putchar(result);
		}
		_putchar('\n');
		
		return (result);
	}
}
