#include <stdlib.h>
#include <stdio.h>
/**
 * main - function that prints the numbers 0 to 9, followed by a new line
 * Return: Always 0 Success
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		_putchar(n);
	_putchar('\n');
}
