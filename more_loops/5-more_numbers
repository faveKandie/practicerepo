#include <stdlib.h>
#include <stdio.h>

/**
* main - function that prints the numbers 0 to 14, followed by a new line, ten times.
* Return: Always 0 Success
*/
int main(void)
{
int n;

for (n = 0; n <= 14; n++)
{
for ((n / 10) + '0')
{
_putchar((n / 10) + '0');
}
_putchar((n % 10) + '0');

_putchar('\n');
}
}
