#include <stdlib.h>
#include <stdio.h>
/**
* main - function that prints the numbers 0 to 9 except for 2 and 4, followed by a new line
* Return: Always 0 Success
*/
int main(void0
{
int n;

for (n = 0; n <= 9; n++)
{
while (n != 2 && n != 4)
_putchar((n % 10) + '0');
}
_putchar('\n');
}
